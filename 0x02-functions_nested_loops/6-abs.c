#include "main.h"

/**
 * _abs - print absolute value
 * Description: print_absolute value.
 * Return: absolute value.
 */
int _abs(int n)
{
	if (n < 1)
	{
		n = -n;
		return (n);
	}
	else 
		return (n);
}
